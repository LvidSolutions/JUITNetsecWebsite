import test from 'node:test';
import assert from 'node:assert/strict';
import { consumeContactRateLimit } from '../api/_lib/rate-limit.js';
const options = (overrides = {}) => ({ request: new Request('https://juitnetsec.se/api/contact', { headers: { 'x-vercel-forwarded-for': '203.0.113.10' } }), email: 'person@example.com', upstashUrl: 'https://redis.example', upstashToken: 'token', hashSecret: '12345678901234567890123456789012', now: 1_800_000, ...overrides });
test('uses separate hashed IP and email keys', async () => { let body; const result = await consumeContactRateLimit(options({ fetchImpl: async (_url, init) => { body = init.body; return Response.json([{ result: 1 }, { result: 1 }, { result: 1 }, { result: 1 }]); } })); assert.equal(body.includes('203.0.113.10'), false); assert.equal(body.includes('person@example.com'), false); assert.deepEqual(result, { allowed: true, remaining: 4, retryAfter: 900 }); });
test('blocks an over-limit email and fails closed for Redis outages', async () => { const limited = await consumeContactRateLimit(options({ fetchImpl: async () => Response.json([{ result: 1 }, {}, { result: 3 }, {}]) })); assert.equal(limited.allowed, false); await assert.rejects(consumeContactRateLimit(options({ fetchImpl: async () => new Response('down', { status: 503 }) })), (error) => error?.status === 503); });
